<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Ted Martin's personal blog">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script type="text/javascript">
      (function(c,l,a,r,i,t,y){
          c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
          t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
          y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "l6zgmksr19");
    </script>
  </head>
  <body>
    <%= render "partials/nav" %>
    <div class="container">
      <div class="container-small">
        <%= yield %>
      </div>
    </div>
    <footer>
      <div class="footer-links">
        <% if current_user %>
          <%= link_to "Admin", admin_posts_path %>
          <%= link_to "Logout", session_path(current_user), class: 'button-primary', data: { turbo_method: :delete } %>
        <% else %>
          <%= link_to "Login", new_session_path %>
        <% end %>
      </div>
      <p>&copy; <%= Time.now.year %> Ted Martin</p>
    </footer>
  </body>
</html>
